## Packages
framer-motion | Page transitions and scroll-triggered animations
@hookform/resolvers | For Zod validation in forms
lucide-react | Beautiful icons

## Notes
- Tailwind Config uses CSS variables for fonts, no `extend.fontFamily` needed.
- Using dark mode color palette by default in `:root` to enforce the requested "dark mode professional" aesthetic.
- Relies on Replit's existing Shadcn UI components in `client/src/components/ui/`.
